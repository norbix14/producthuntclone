import styled from '@emotion/styled'

export const Formulario = styled.form`
	max-width: 600px;
	width: 95%;
	margin: 5rem auto;
	fieldset {
		margin: 2rem 0;
		border: 1px solid var(--gris3);
		font-size: 2rem;
		padding: 1.5rem;
	}
`
